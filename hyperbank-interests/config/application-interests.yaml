spring:
   application:
      name: hyperbank-interests
      version: 1.0.0
   profiles:
      include: config_main, config_storage, config_swagger
   config:
      import: configserver:http://${env.config-provider-host:localhost}:${env.services-server-port.hyperbank-config-provider}/config
   cloud:
     config:
       fail-fast: true
       
server.port: ${env.services-server-port.hyperbank-interests}