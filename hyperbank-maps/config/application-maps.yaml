spring:
   application:
      name: hyperbank-maps
      version: 1.0.0
   profiles:
      include: config_main, config_storage, config_swagger
   config:
      import: configserver:http://${env.config-provider-host:localhost}:${env.services-server-port.hyperbank-config-provider:8888}/config
   cloud:
     config:
       fail-fast: true
       
server.port: ${env.services-server-port.hyperbank-maps}